{
  "name": "@ondemandenv.dev/contracts-lib-base",
  "repository": {
    "url": "https://github.com/ondemandenv/odmd-contracts-base"
  },
  "version": "0.1.7",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [ ".odmd", "dist", "readme.md" ],
  "dependencies": {
    "aws-cdk-lib": "2.203.1",
    "constructs": "^10.0.0",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "22.7.9",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "~5.6.3",
    "@types/js-yaml": "^4.0.9",
    "cross-env": "^7.0.3",
    "glob": "^11.0.0"
  },
  "peerDependencies": {
    "constructs": "^10.0.0",
    "aws-cdk-lib": "2.203.1"
  },
  "scripts": {
    "build": "npm run gen-exports && tsc --build",
    "watch": "tsc --build -w",
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "audit": "npm audit fix",
    "test": "npm run clean && npm run build && cross-env target_rev_ref=b..odmdSbxUsw1 CDK_DEFAULT_ACCOUNT=123 CDK_DEFAULT_REGION=us-west-1 CDK_CLI_VERSION=2.163.0 jest",
    "buildversion": "tsc --build && npm version patch",
    "cdk-ls": "npm run clean && npm run build && cross-env target_rev_ref=b..main cdk ls -v --profile sandbox-workspace3",
    "pubpub-njm": "npm run package && npm publish --access=public --verbose",
    "package": "npm run test && npm run gen-exports && npm pack",
    "login": "npm login",
    "gen-exports": "ts-node scripts/generate-exports.ts"
  }
}
