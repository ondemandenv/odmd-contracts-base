{
  "name": "@ondemandenv.dev/contracts-lib-base",
  "repository": {
    "url": "https://github.com/ondemandenv/odmd-contracts-base"
  },
  "version": "0.1.11",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [ ".odmd", "dist", "readme.md" ],
  "dependencies": {
    "aws-cdk-lib": "2.221.1",
    "constructs": "^10.0.0",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "22.7.9",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "~5.6.3",
    "@types/js-yaml": "^4.0.9",
    "cross-env": "^7.0.3",
    "glob": "^11.0.0",
    "cpx": "^1.5.0"
  },
  "peerDependencies": {
    "constructs": "^10.0.0",
    "aws-cdk-lib": "2.221.1"
  },
  "scripts": {
    "prebuild": "npm run gen-exports",
    "build": "tsc --build &&  cpx \"lib/repos/**/docs/**/*\" dist/lib/repos || true",
    "watch": "tsc --build -w",
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "audit": "npm audit fix",
    "test": "npm run clean && npm run build && cross-env target_rev_ref=b..odmdSbxUsw1 CDK_DEFAULT_ACCOUNT=123 CDK_DEFAULT_REGION=us-west-1 CDK_CLI_VERSION=2.163.0 jest",
    "buildversion": "tsc --build && npm version patch",
    "cdk-ls": "npm run clean && npm run build && cross-env target_rev_ref=b..main cdk ls -v --profile sandbox-workspace3",
    "package": "npm run test && npm run build && npm pack",
    "pubpub-njm": "npm run test && npm run package && npm publish --access=public --verbose",
    "login": "npm login",
    "gen-exports": "ts-node scripts/generate-exports.ts"
  }
}
